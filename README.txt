Para executar o codigo, baixe o SQL_CRUD, execute o codigo
do arquivo no mysql. Em seguida, importe a pasta CRUD para
um compilador java (preferencialmente no eclipse, já que
foi neste em que o codigo foi desenvolvido)